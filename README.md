# AIMETON AKID Viewer

Автономный офлайн-viewer для мониторинга работ АКИД/АКИП.

## Презентация продукта

- [Корпоративный UX Viewer — публичная презентационная страница](SHOWCASE.md) — проблема, подход Enterprise UX Overlay, возможности продукта, сценарий применения, безопасность и ссылка на актуальный Release.

## Документация

- [Руководство пользователя](docs/USER_GUIDE_RU.md) — первый запуск, разблокировка Windows, работа с XLSX/CSV, фильтры, группировка, назначения, выдачи, экспорт, конфигурации, восстановление последних файлов и решение типовых проблем.
- [Раздел документации](docs/README.md) — навигация по пользовательской и технической документации.
- [Windows 7 Legacy build](docs/WINDOWS7_LEGACY.md) — отдельная совместимая сборка, ограничения и порядок приёмки.

## Возможности линии v1.3

- на одну задачу можно назначать несколько исполнителей;
- старые конфигурации с одним назначенным исполнителем остаются совместимыми;
- в поле **Группировка** доступен вариант **Назначенные исполнители**;
- задача с несколькими исполнителями отображается в группе каждого назначенного человека;
- неназначенные задачи попадают в группу **Без исполнителя**;
- персональные выдачи учитывают совместные назначения;
- экспорт XLSX содержит перечень всех назначенных исполнителей;
- встроенное руководство открывается кнопкой `[?]` и отображается как отрендерированная справка.

## Portable EXE для Windows

При публикации Release собираются две отдельные x64-сборки:

- `AIMETON_AKID_Viewer_<version>_Windows_x64.exe` — основная сборка для Windows 10/11 на современной ветке Electron;
- `AIMETON_AKID_Viewer_<version>_Windows7_x64.exe` — Legacy-сборка на Electron 22.3.27 для Windows 7 x64.

Обе сборки portable: установка, Node.js и доступ в Интернет не требуются.

Windows 7 build является legacy-вариантом, потому что Electron 22 больше не поддерживается upstream. Его следует использовать только там, где обновление ОС пока невозможно, и отдельно проверять на реальном корпоративном ПК с Windows 7.

В Release также прикладываются:

- `UNBLOCK_AKID_VIEWER.bat` — снятие Windows Internet-zone marker без глобального отключения SmartScreen;
- `AIMETON_AKID_Viewer_User_Guide_RU.pdf` — PDF-руководство пользователя.

Release tag должен иметь вид `v1.2.3` или `1.2.3`. Версия автоматически переносится в имена EXE.

Сборка выполняется workflow `.github/workflows/build-windows-exe.yml` на Windows runner и после успешной проверки автоматически прикрепляется к опубликованному GitHub Release.

## Автоматическая загрузка рабочих файлов

Windows EXE упрощает повторный запуск:

1. при старте сначала ищет файл `employees.csv` рядом с EXE и, если находит, автоматически загружает его;
2. если `employees.csv` рядом с EXE нет, пытается открыть последний ранее выбранный CSV исполнителей;
3. запоминает путь последнего выбранного XLSX с работами и при следующем запуске автоматически пытается загрузить его;
4. запоминает путь последнего выбранного CSV исполнителей;
5. если сохранённый файл был перемещён или удалён, Viewer не падает — пользователь может выбрать новый файл вручную.

Пути последних файлов хранятся локально в пользовательском каталоге приложения, а сами рабочие XLSX/CSV никуда не копируются и не отправляются.

## Главное свойство: работа без Интернета

Runtime приложения полностью локальный. EXE содержит Viewer и его локальные зависимости; backend не требуется.

Исходная браузерная версия также остаётся автономной. После клонирования или скачивания репозитория она не обращается к внешним CDN.

Все runtime-зависимости хранятся локально в `lib/`:

- Tailwind browser runtime;
- SheetJS/XLSX;
- Font Awesome и необходимые webfonts.

Для браузерного запуска достаточно открыть `index.html` в современном браузере.

Пользовательские настройки, фильтры и назначения сохраняются локально.

## Разработка и упаковка

Electron используется только как оболочка для локального Viewer. В renderer отключена Node.js integration, включены `contextIsolation`, `sandbox` и `webSecurity`. Доступ к локальным файлам для автозагрузки выполняется через узкий preload/IPC-мост.

Основная локальная упаковка Windows:

```bash
npm install
npm run build:win
```

Windows 7 build формируется CI отдельно с Electron 22.3.27 и legacy preload-мостом.

## Данные

Рабочие XLSX/CSV из исходного архива намеренно не публикуются в этом публичном репозитории: они могут содержать персональные и внутренние рабочие сведения. Viewer работает с файлами, которые пользователь выбирает локально на своём компьютере.

## AIMETON

Прикладное инженерное решение разработано Инженерной AI-студией AIMETON.

- Email: office@aimeton.ru
- Web: https://www.aimeton.ru

© 2026 AIMETON.
